<% unless @bookmarks %>

  <h1 class="title special">Readabilify</h1>
  <h3 class="subtitle special">Abilifying Readability</h3>

  <div class="copy">
    <p>
      So you like Readability, right? Me to. But there's those times when I'd love to have copies of my saved articles in PDF, 
      or other hard copy format, even let it sync to various offline locations so I can read them when I'm not connected. What to do?
    </p>

    <p>
      Use Readabilify! We give your artcles legs to walk into any format and onto any device or easily re-formattable and printable 
      hard copy! All you have to do is connect with your Readability account and <b>BOOM</b>, your artcles can go anywhere you can, 
      however you want them to. All you have to do is...
    </p>
  </div>

  <h2 class="special">Login with</h2>
  <%= link_to image_tag("readability-sm.png"), new_readability_session_path, class: 'login button' %>

<% else %>
  
  <div class="filter">
    Filter List
    <%= form_tag home_path, method: :get do |f| %>
      <ul>
        <li><%= check_box_tag :archive, '1', params[:archive] == '1' %> Show Archived</li>
        <li><%= check_box_tag :favorite, '1', params[:favorite] == '1' %> Favorites</li>
      </ul>
      <br/>
      <%= submit_tag "Update", class: 'button' %>
    <% end %>
  </div>

  <table class="article-list">
    <thead>
      <tr>
        <th>Title</th>
        <th class="filter-show"><a href="#"><%= image_tag "gear.png" %></a></th>
      </tr>
    </thead> 
    <% @bookmarks.each do |article| %>
      <tr>
        <td class="titles"><%= article['article']['title'] %></td>
        <td class="links">
          <%= format_link(article, :pdf) %>
          <%= format_link(article, :epub) %>
          <%= format_link(article, :mobi) %>
        </td>
      </tr>
    <% end %>
  </table>

  <%= link_to "Disconnect from Readability", home_path, method: :delete %>
<% end %>
